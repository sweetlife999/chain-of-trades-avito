# Объявления: CRUD

Объявление — это вещь, которую владелец готов обменять, вместе со списком категорий,
которые он хочет получить взамен. Из этих желаний собирается граф обмена, поэтому
объявление без них бесполезно для подбора обменов.

CRUD — четыре операции:

- **Create** — создать объявление;
- **Read** — получить объявление по ID или список своих;
- **Update** — изменить объявление;
- **Delete** — удалить объявление.

В отличие от [пользователей](users.md), удаление здесь есть: вещь можно передумать менять.

## Маршруты

| Метод и путь | Что делает | Авторизация | Успешный ответ |
|---|---|---|---|
| `POST /items` | Создаёт объявление | нужна | `201 Created` |
| `GET /items` | Возвращает свои объявления | нужна | `200 OK` |
| `GET /items/{id}` | Возвращает объявление по UUID | не нужна | `200 OK` |
| `PATCH /items/{id}` | Изменяет переданные поля | нужна, только своё | `200 OK` |
| `DELETE /items/{id}` | Удаляет объявление | нужна, только своё | `204 No Content` |
| `PUT /items/{id}/search` | Возвращает снятое объявление в поиск | нужна, только своё | `200 OK` |
| `DELETE /items/{id}/search` | Снимает объявление с поиска | нужна, только своё | `200 OK` |
| `POST /items/{id}/pickup` | Отмечает, что вещь отнесли в ПВЗ | нужна, только своё | `204 No Content` |
| `DELETE /items/{id}/pickup` | Возвращает вещь домой | нужна, только своё | `204 No Content` |
| `GET /categories` | Справочник категорий | не нужна | `200 OK` |

Защищённые маршруты читают cookie `access_token` — см. [документацию авторизации](auth.md).

## Категории

Категория указывается слагом, а не числовым id: слаг стабилен и не зависит от
переименования. Список берётся из справочника:

```http
GET /categories
```

```json
[
  {"slug": "bikes", "name": "Велосипеды и транспорт"},
  {"slug": "consoles", "name": "Игровые приставки"}
]
```

Неизвестный слаг — `400 Bad Request`, а не тихая подстановка чего-нибудь другого.

## Создание объявления

```http
POST /items
Content-Type: application/json
```

```json
{
  "category": "bikes",
  "title": "Велосипед",
  "description": "Почти новый, катался год",
  "photo_urls": [
    "/uploads/8db9f3e2-8a45-4a70-b3d1-167b4f97e121.jpg",
    "/uploads/2f0b6c7a-1e11-4a0a-9d0f-6f1c1c531f0e.jpg"
  ],
  "wants": ["consoles", "phones"]
}
```

Обязательны `category`, `title`, `photo_urls` и `wants`; `description` можно не передавать.

Фотографии загружаются заранее — по одной через `POST /uploads`, который возвращает путь
(см. [uploads.md](uploads.md)). Внешние `http`/`https`-ссылки тоже принимаются.

Владелец берётся из cookie, а не из тела запроса: создать объявление от чужого имени
нельзя. Успешный ответ содержит созданное объявление и заголовок `Location: /items/{id}`.
Статус нового объявления — `available`.

Сразу после сохранения service запускает поиск обмена от нового объявления. Отдельная
кнопка и отдельный HTTP-запрос для этого не нужны. Если подходящего обмена пока нет,
объявление всё равно успешно создаётся и остаётся доступным для следующих поисков.

## Получение объявления

```http
GET /items/8db9f3e2-8a45-4a70-b3d1-167b4f97e121
```

```json
{
  "id": "8db9f3e2-8a45-4a70-b3d1-167b4f97e121",
  "owner_id": "c80a1712-1cf2-4361-9711-39f212d404c7",
  "category": "bikes",
  "title": "Велосипед",
  "description": "Почти новый, катался год",
  "photo_urls": [
    "/uploads/8db9f3e2-8a45-4a70-b3d1-167b4f97e121.jpg",
    "/uploads/2f0b6c7a-1e11-4a0a-9d0f-6f1c1c531f0e.jpg"
  ],
  "wants": ["consoles", "phones"],
  "status": "available",
  "pickup_point": null,
  "created_at": "2026-08-05T01:47:11Z",
  "updated_at": "2026-08-05T01:47:11Z"
}
```

Карточка публичная: авторизация не нужна.

`pickup_point` — пункт выдачи, в котором лежит вещь, или `null`, если она дома у владельца.
Отмечают его отдельные маршруты, см. [документ о пунктах выдачи](pickup-points.md).

## Список своих объявлений

```http
GET /items
```

Владелец берётся из cookie: маршрут отдаёт только объявления того, кто спрашивает, и
чужой список получить нельзя. Ответ — массив ровно таких же карточек, как выше, от новых
к старым.

Отдаются объявления всех статусов, включая занятые в обмене: список — это «мои вещи»,
а не «мои доступные вещи». Если объявлений нет, ответ — пустой массив `[]`, а не `null`.

## Изменение объявления

`PATCH` меняет только переданные поля, остальные остаются прежними.

```http
PATCH /items/8db9f3e2-8a45-4a70-b3d1-167b4f97e121
Content-Type: application/json
```

```json
{
  "title": "Велосипед городской",
  "photo_urls": [
    "/uploads/8db9f3e2-8a45-4a70-b3d1-167b4f97e121.jpg",
    "/uploads/2f0b6c7a-1e11-4a0a-9d0f-6f1c1c531f0e.jpg",
    "/uploads/167b4f97-e121-4a70-b3d1-8db9f3e28a45.jpg"
  ]
}
```

Менять можно `category`, `title`, `description`, `photo_urls` и `wants`. Нужно передать
хотя бы одно поле. Статус объявления этим маршрутом не меняется — им распоряжается механика
обменов.

Изменение `category` или `wants` меняет совместимость объявления, поэтому после такого
обновления поиск запускается повторно. Изменения названия, описания и фотографий на поиск
не влияют. Условия объявления, которое уже участвует в незавершённом обмене, менять нельзя:
API вернёт `409 Conflict`.

Считается изменение значения, а не наличие поля в запросе: `category` и `wants` можно
слать с теми же значениями, что уже сохранены, — это не смена условий, и клиент, который
отправляет объявление целиком, `409` на правке названия не получит. Порядок `wants`
роли не играет.

**Списки заменяются целиком.** `photo_urls` и `wants` — это не «добавить к тому, что есть»,
а «теперь ровно это». Отсюда все сценарии работы с фотографиями:

| Что нужно | Что передать |
|---|---|
| Добавить одну фотографию | старые ссылки + новую (сначала загрузить её) |
| Добавить несколько сразу | старые ссылки + новые |
| Поменять порядок | те же ссылки в другом порядке: первая — обложка |
| Удалить одну из трёх | две оставшиеся |
| Заменить все фотографии | только новые ссылки |
| Удалить все фотографии | невозможно: `[]` → `400` |

Пустой список и в `photo_urls`, и в `wants` — ошибка. Отсутствие поля в JSON и пустой
список различаются: первое означает «не трогай», второе — «сделай пустым», и второе
запрещено.

## Удаление объявления

```http
DELETE /items/8db9f3e2-8a45-4a70-b3d1-167b4f97e121
```

Успех — `204 No Content` без тела. Удалять можно только свои объявления.

Вещь, уже участвующую в незавершённом обмене, удалить нельзя — `409 Conflict`. Иначе участник
обмена остался бы без обещанного предмета. Запрет держит внешний ключ
`chain_participants → items ON DELETE RESTRICT`, то есть его нельзя обойти мимо API.
Желания удалённой вещи (`item_wants`) уходят каскадом.

## Снятие объявления с поиска

Снять объявление без удаления:

```http
DELETE /items/8db9f3e2-8a45-4a70-b3d1-167b4f97e121/search
```

Объявление получает статус `withdrawn` и перестаёт быть вершиной графа поиска. Все ещё не
подтверждённые `proposed`-обмены с этой вещью атомарно отменяются. Их участники увидят в
чате событие `exchange_item_withdrawn`, а для оставшихся доступных объявлений backend
запустит новый подбор.

Подтверждённый обмен уже зарезервировал вещь, поэтому снять её нельзя — API вернёт
`409 Conflict`. Проверка выполняется в одной транзакции с изменением статуса и использует
тот же lock, что подтверждение обмена: одновременные запросы не могут оба успешно забрать
одну вещь.

Вернуть объявление:

```http
PUT /items/8db9f3e2-8a45-4a70-b3d1-167b4f97e121/search
```

Статус меняется с `withdrawn` на `available`, после чего автоматически запускается поиск.
Обе ручки идемпотентны: повторное снятие или повторная публикация возвращают актуальную
карточку с `200 OK` и не создают лишних поисковых заданий.

## Проверка данных

- `title` после очистки — от 1 до 120 символов;
- `category` — слаг из справочника `GET /categories`;
- `photo_urls` — от 1 до 10 ссылок, каждая либо путь загруженного файла (`/uploads/<файл>`,
  выдаёт `POST /uploads`, см. [uploads.md](uploads.md)), либо абсолютный `http`/`https`-адрес;
  прочие относительные пути и другие схемы отвергаются, пустые строки отбрасываются;
- `wants` — от 1 до 10 слагов; регистр приводится к нижнему, дубликаты отбрасываются;
- ID в адресе должен быть корректным UUID;
- неизвестные поля в JSON не принимаются;
- тело запроса должно содержать ровно один JSON-объект и быть не больше 1 МБ.

Ограничение «хотя бы одна фотография» продублировано в БД
(`CHECK (cardinality(photo_urls) BETWEEN 1 AND 10)`), поэтому оно действует и для запросов
мимо API.

## Коды ответа

| Код | Когда возвращается |
|---|---|
| `200 OK` | Объявление или список получены, объявление обновлено, снято с поиска или возвращено в него |
| `201 Created` | Объявление создано |
| `204 No Content` | Объявление удалено |
| `400 Bad Request` | Некорректный UUID или JSON, данные не прошли проверку, неизвестная категория, пустой список фото или желаний |
| `401 Unauthorized` | Нет или истекла cookie `access_token` |
| `403 Forbidden` | Объявление принадлежит другому пользователю |
| `404 Not Found` | Объявления с таким ID нет |
| `409 Conflict` | Вещь участвует в незавершённом обмене, меняются её условия или она уже вне оборота |
| `500 Internal Server Error` | Непредвиденная ошибка на стороне backend |

Ошибки возвращаются в одном формате:

```json
{
  "error": "item not found"
}
```

`403` и `404` не смешиваются: владелец проверяется чтением объявления, а не условием в
`UPDATE`, иначе «чужая вещь» и «нет такой вещи» дали бы один и тот же ответ.

## Как запрос проходит через приложение

```text
HTTP-запрос
    ↓
handler — читает JSON, берёт владельца из cookie, формирует HTTP-ответ
    ↓
service — очищает данные, проверяет правила и права на объявление
    ↓
repository — выполняет запросы к PostgreSQL, вещь и желания пишет одной транзакцией
    ↓
sqlc — генерирует типобезопасный Go-код из queries/items.sql
```

Слои те же, что у пользователей, и делят работу так же (см. [users.md](users.md)).
Отличие одно: объявление живёт в двух таблицах (`items` и `item_wants`), поэтому создание и
изменение идут в транзакции. Без неё неизвестный слаг в `wants` оставил бы уже изменённые
поля объявления в БД при ответе `400`.

## Ограничения текущей версии

- фотографии хранятся ссылками: файлы загружаются отдельным запросом `POST /uploads`, а сюда
  приезжает путь к ним (см. [uploads.md](uploads.md)). Внешние `http`/`https`-адреса тоже
  принимаются — с ними живут объявления, созданные раньше;
- список отдаётся целиком: фильтров, сортировки параметром и пагинации пока нет;
- чужих и общих списков нет — только свои объявления и карточка по ID;
- `status` через API не меняется — им управляет механика обменов;
- владелец вещи после завершённого обмена не меняется: вещь остаётся в профиле прежнего
  хозяина со статусом `traded`.

## Запуск и проверка

```bash
cp .env.example .env
make up       # поднять PostgreSQL и применить миграции
make run      # запустить API на localhost:8080
go test ./... # запустить тесты
```

Быстрая проверка (после `POST /auth/login` cookie лежит в `cookies.txt`):

```bash
curl -s -c cookies.txt -X POST http://localhost:8080/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"nickname":"Samir","password":"password123"}'

curl -s -b cookies.txt -X POST http://localhost:8080/uploads -F "file=@bike.jpg"
# {"url":"/uploads/8db9f3e2-8a45-4a70-b3d1-167b4f97e121.jpg"}

curl -i -b cookies.txt -X POST http://localhost:8080/items \
  -H 'Content-Type: application/json' \
  -d '{"category":"bikes","title":"Велосипед","photo_urls":["/uploads/8db9f3e2-8a45-4a70-b3d1-167b4f97e121.jpg"],"wants":["consoles"]}'

curl -s -b cookies.txt http://localhost:8080/items   # свои объявления
```
