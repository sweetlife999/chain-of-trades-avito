-- +goose Up

-- Когда владелец снимает объявление с поиска, все предложения с этой вещью закрываются.
-- Отдельный kind позволяет фронту объяснить участникам причину, а не показывать безликое
-- «обмен отменён». Значение добавляется отдельно от использующих его запросов: PostgreSQL
-- не разрешает использовать новый enum value до коммита миграции.
ALTER TYPE chain_message_kind ADD VALUE IF NOT EXISTS 'exchange_item_withdrawn';

-- +goose Down

-- PostgreSQL не поддерживает DROP VALUE. После отката код перестаёт создавать событие,
-- а лишнее допустимое значение не меняет существующие строки и ограничения.
SELECT 1;
