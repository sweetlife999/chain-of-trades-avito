import { memo } from "react"

const PostsListComponent = () => {
    return(
        <ul>
            <li> {/*Link на конкретный обмен(post/:id)*/}

            </li>
        </ul>
    )
}

export const PostsList = memo(PostsListComponent)